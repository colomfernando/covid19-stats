import React, { ReactElement } from 'react';

const App: React.FC = (): ReactElement => (
  <div className="App">
    <header className="App-header">
      <p>covid</p>
    </header>
  </div>
);

export default App;
